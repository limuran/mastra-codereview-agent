name = "mastra-codereview-standalone"
main = "src/cloudflare/standalone-worker.ts"
compatibility_date = "2024-01-01"
node_compat = true

[vars]
# No Mastra server needed for standalone mode

# Add your secrets using: wrangler secret put ANTHROPIC_API_KEY
# This is the only secret needed for standalone mode